import 'package:flutter/material.dart';

class AdminNotificationFragmentScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            "Admin Notification Fragment Screen"
        ),
      ),
    );
  }
}
