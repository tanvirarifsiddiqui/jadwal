import 'package:flutter/material.dart';

class AnnouncementFragmentScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            "Announcement Fragment Screen"
        ),
      ),
    );
  }
}
